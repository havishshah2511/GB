fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-multipart>=0.0.9

# Optional: enables the Claude NLU extractor. Without it (or without
# ANTHROPIC_API_KEY) the app falls back to the deterministic rules engine.
anthropic>=0.40

# Tests
pytest>=8.0
httpx>=0.27
